[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = []

[phases.start]
cmds = ["cd backend && node server.js"]

[start]
cmd = "cd backend && node server.js"